import React from "react";

const InputWarning = ({ content }) => {
  return (
    <div className="text-red-600 font-inter_regular text-sm mt-1">
      {content}
    </div>
  );
};

export default InputWarning;
